퀴즈 1) 아래 설명해 주세요.

팬시검색을 튜플로 묶어서 순서쌍을 만들어 검색한다
c[tuple([[1,3,4], [3,4,5]])] 첫 번째 행 index에 3열의 포함되는 값, 3행 4열에 포함되는 값, 4행 5열에 포함되는 값을 검색한다
c 배열에 1열의 값을 검색, 3행 4열의 값을 검색, 4행 5열의 값을 검색한다 q

퀴즈 2) 팬시 인덱싱이란?

일정한 인덱싱 집합을 리스트 또는 ndarray 형태로 지정해 해당 위치에 있는 데이터의 ndarray를 반환한다.

퀴즈 3) 파이썬 시퀀스 자료형에 대해서 설명하세요.

파이썬에서 리스트는 넓게 보면 시퀀스(sequence) 자료형에 속하며, 시퀀스에 속하는 자료형들은 순서를 가진 요소들의 집합이라는 공통적인 특성을 가지고 있다. 공식적으로 시퀀스 자료형에는 다음과 같은 6가지의 자료형이 있다.

문자열
바이트 시퀀스
바이트 배열
리스트
튜플
range 객체 (시퀀스 연산들이 적용 안되는 것이 많음)
